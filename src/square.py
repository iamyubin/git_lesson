def square(y):
    """
    Takes a value and returns the squared value,
    Uses the ** operator.
    """
    return y ** 2

def square2(x):
    """
    uses the * operator to calculate square
    """
    return x * x

print(square(10))
print(square2(10))
